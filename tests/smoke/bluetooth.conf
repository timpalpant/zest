CONFIG_ZEST_BLUETOOTH_MANAGER=y

# ZEST_BLUETOOTH_MANAGER depends on BT and a role, so without these the symbol
# is simply unavailable, the component is compiled out, and this configuration
# silently tests nothing.
CONFIG_BT=y
CONFIG_BT_PERIPHERAL=y
CONFIG_BT_CENTRAL=y
CONFIG_BT_MAX_CONN=1
